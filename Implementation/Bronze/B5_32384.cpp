#include <bits/stdc++.h>

using namespace std;

int main(void) {

    // 입출력 고속화
    ios::sync_with_stdio(false), cin.tie(NULL);

    // 반복 횟수 입력 받기
    int N;
    cin >> N;

    // 횟수만큼 출력 반복
    for (int i = 0; i < N; i++) {
        cout << "LoveisKoreaUniversity ";
    }
    cout << '\n';

    return 0;
}